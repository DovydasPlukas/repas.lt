'use client';
import React from 'react'

const TestPrideti = () => {
  return (
    <div>
      <button onClick={() => console.log('Click')}>Prideti paslauga</button>
    </div>
  )
}

export default TestPrideti
